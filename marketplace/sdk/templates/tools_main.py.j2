"""Main tool implementation for {{ skill_name }}."""


def run(**kwargs) -> dict:
    """
    Entry point for the {{ skill_name }} skill.

    Args:
        **kwargs: Arguments declared in manifest.yaml → tools[].parameters

    Returns:
        dict with at least {"result": ...}
    """
    # TODO: implement {{ skill_name }}
    return {"result": "Hello from {{ skill_name }}!"}